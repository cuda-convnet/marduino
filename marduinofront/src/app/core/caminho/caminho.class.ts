export class Caminho {
    private descricao: string;
    private rota: string;

    constructor(descricao: string, rota: string) {
        this.descricao = descricao;
        this.rota = rota;
    }

}
